name: Docker — Build & Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build & Push image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU (multi-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/crocdeine/ministere-de-l-info
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/Dockerfile
          platforms: linux/arm64,linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # TODO: La DB n'est pas dans le repo (données trop lourdes, data/ est dans .gitignore).
  # Pour distribuer la DB initiale, la télécharger manuellement depuis la machine de
  # développement et l'attacher à la release via :
  #
  #   gzip -k data/ministere.duckdb
  #   shasum -a 256 data/ministere.duckdb > data/ministere.duckdb.gz.sha256
  #   gh release upload <tag> data/ministere.duckdb.gz data/ministere.duckdb.gz.sha256
  #
  # Voir deploy/README-deploy.md pour la procédure complète.
  #
  # export-db:
  #   name: Export & attach DB
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   if: startsWith(github.ref, 'refs/tags/v')
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     # La DB n'est pas dans le repo — ce job ne peut pas s'exécuter automatiquement.
  #     # Procédure manuelle documentée ci-dessus.
